# Backend Configuration - Environment Variables
# Copy this file to .env and fill in your actual values (DO NOT commit .env to version control)

# Server Configuration
PORT=5000
NODE_ENV=development
# Options: development, production

# Logging Configuration
LOG_LEVEL=info
# Options: error, warn, info, http, debug

# Database Configuration
MONGO_URI=mongodb+srv://username:password@cluster.mongodb.net/database?retryWrites=true&w=majority
# Use a strong, unique password and ensure the IP whitelist allows your server

# Authentication (JWT)
JWT_SECRET=your-super-secret-jwt-key-here-min-32-chars-recommended
# IMPORTANT: Generate a strong random string (at least 32 characters) using:
# node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
# NEVER use default values in production!

# TLS/HTTPS Certificates (Optional - for production HTTPS)
# TLS_CERT=/path/to/server.crt
# TLS_KEY=/path/to/server.key
# If not provided, server will run over HTTP (development) or fail in production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5173
# Production: https://yourdomain.com
# Staging: https://staging.yourdomain.com,http://localhost:5173
# Comma-separated list of allowed origins

# Security Configuration (Security Misconfiguration Prevention)
# Content Security Policy
CSP_ENABLED=true
# CSP_DIRECTIVE=default-src 'self'; script-src 'self'

# Error Handling
SHOW_ERROR_DETAILS=true
# Set to false in production to hide detailed error messages

# Request Timeouts (in milliseconds)
REQUEST_TIMEOUT=30000
RESPONSE_TIMEOUT=30000

# Debug Mode (CAREFUL: Disable in production!)
DEBUG=false
# Never enable in production - exposes stack traces and sensitive info

# Email Configuration (Optional - if you implement email notifications)
# EMAIL_SERVICE=gmail
# EMAIL_USER=your-email@gmail.com
# EMAIL_PASSWORD=your-app-specific-password

# Security Features
# API_RATE_LIMIT=100
# Set rate limit per minute for API endpoints (optional)

# Database Backup (Optional)
# BACKUP_ENABLED=false
# BACKUP_SCHEDULE=0 2 * * * (cron format - daily at 2 AM)
