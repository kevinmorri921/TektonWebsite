# 🏗️ Security Architecture Overview

## Complete Security Implementation Architecture

This document provides a visual and detailed overview of how all security features work together.

---

## 🔄 Request Flow with Security

```
┌─────────────────────────────────────────────────────────────────┐
│                         Client Request                          │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
                ┌────────────────────────────┐
                │   CORS Validation          │
                │   Origin Checking          │
                └────────────┬───────────────┘
                             │
                             ▼
                ┌────────────────────────────┐
                │   Security Headers         │
                │   - HSTS                   │
                │   - CSP                    │
                │   - X-Frame-Options        │
                │   - X-XSS-Protection       │
                └────────────┬───────────────┘
                             │
                             ▼
                ┌────────────────────────────┐
                │   Request Logging          │
                │   (with sanitization)      │
                └────────────┬───────────────┘
                             │
                             ▼
                ┌────────────────────────────┐
                │   Data Integrity Attach    │
                │   (attach helpers)         │
                └────────────┬───────────────┘
                             │
                    ┌────────┴──────────┐
                    │                   │
                    ▼                   ▼
          ┌──────────────────┐  ┌──────────────────┐
          │  Auth Required   │  │  Public Endpoint │
          │  Route?          │  │  (proceed)       │
          └────────┬─────────┘  └──────────────────┘
                   │                       │
         ┌─────────┴─────────┐             │
         │                   │             │
         ▼                   ▼             │
   ┌──────────┐      ┌─────────────┐      │
   │ Validate │      │ Validate    │      │
   │ Token    │      │ Permissions │      │
   └────┬─────┘      └─────┬───────┘      │
        │                  │              │
        └──────────┬───────┘              │
                   │                      │
                   ▼                      │
         ┌─────────────────────┐          │
         │  Role Authorization │          │
         │  (RBAC)             │          │
         └──────────┬──────────┘          │
                    │                     │
         ┌──────────┴─────────────────────┤
         │                                │
         ▼                                ▼
┌─────────────────────┐    ┌─────────────────────┐
│ Input Validation    │    │ Input Validation    │
│ - Whitelist schema  │    │ - Whitelist schema  │
│ - Type checking     │    │ - Type checking     │
│ - Format validation │    │ - Format validation │
└──────────┬──────────┘    └──────────┬──────────┘
           │                          │
           ▼                          ▼
┌─────────────────────┐    ┌─────────────────────┐
│ Input Sanitization  │    │ Input Sanitization  │
│ - removeXSS()       │    │ - removeXSS()       │
│ - escapeHtml()      │    │ - escapeHtml()      │
│ - Recursive clean   │    │ - Recursive clean   │
└──────────┬──────────┘    └──────────┬──────────┘
           │                          │
           ▼                          ▼
┌─────────────────────┐    ┌─────────────────────┐
│ Business Logic      │    │ Business Logic      │
│ Execution           │    │ Execution           │
└──────────┬──────────┘    └──────────┬──────────┘
           │                          │
           ▼                          ▼
┌─────────────────────┐    ┌─────────────────────┐
│ Database Query      │    │ Database Query      │
│ (Parameterized)     │    │ (Parameterized)     │
│ ✅ NoSQL injection  │    │ ✅ NoSQL injection  │
│    prevention       │    │    prevention       │
└──────────┬──────────┘    └──────────┬──────────┘
           │                          │
           ▼                          ▼
┌─────────────────────────────────────────────┐
│        Data Integrity Verification          │
│  - Calculate/verify checksums               │
│  - Check tampering                          │
│  - Calculate hash for audit                 │
└──────────────┬────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│        Response Processing                  │
│  - Output encoding (escapeHtml)             │
│  - Safe error messages (no stack traces)    │
│  - Add integrity metadata                   │
└──────────────┬────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│        Response Logging                     │
│  - Log success/failure                      │
│  - Record performance                       │
│  - Track audit events                       │
└──────────────┬────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────┐
│           Client Response                   │
│  ✅ Secure | ✅ Validated | ✅ Safe        │
└─────────────────────────────────────────────┘
```

---

## 🗄️ Security Middleware Stack

```
                      Express App
                           │
                           ▼
    ┌──────────────────────────────────────────┐
    │          CORS Middleware                 │
    │    (Origin: localhost:5173)              │
    └──────────────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────┐
    │       Security Headers Middleware         │
    │  - HSTS (1 year)                         │
    │  - CSP (self-origin)                     │
    │  - X-Frame-Options (DENY)                │
    │  - X-Content-Type-Options (nosniff)      │
    │  - X-XSS-Protection (1; mode=block)      │
    └──────────────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────┐
    │       Request Logging Middleware          │
    │  - Log all requests                      │
    │  - Scrub sensitive fields                │
    │  - Send to Winston logger                │
    └──────────────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────┐
    │    Data Integrity Middleware              │
    │  - Attach integrity helper methods       │
    │  - Calculate/verify hashes               │
    │  - Track versions                        │
    └──────────────────────────────────────────┘
                           │
                           ▼
    ┌──────────────────────────────────────────┐
    │          Routes Begin                    │
    │  (Authentication, Validation, etc.)      │
    └──────────────────────────────────────────┘
```

---

## 🔐 Security Layers by Feature

### Layer 1: Logging & Monitoring
```
┌─────────────────────────────────────────────────────────┐
│                   Winston Logger                        │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Input: Request/Response/Events                        │
│           │                                             │
│           ▼                                             │
│  Scrubbing: Remove passwords, tokens, secrets          │
│           │                                             │
│           ▼                                             │
│  Format: {timestamp, level, context, message}          │
│           │                                             │
│           ├──────────▶ combined.log (all)              │
│           │                                             │
│           └──────────▶ error.log (errors only)         │
│                                                         │
│  Output: Structured logs for analysis                  │
└─────────────────────────────────────────────────────────┘
```

### Layer 2: Cryptographic Security
```
┌─────────────────────────────────────────────────────────┐
│              Cryptographic Failures Prevention          │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Secrets Management:                                    │
│  ├─ JWT_SECRET: .env only (no fallback)               │
│  ├─ MONGO_URI: .env only                              │
│  └─ Lazy loading: After dotenv initializes            │
│                                                         │
│  Password Hashing:                                      │
│  ├─ Algorithm: bcrypt                                  │
│  ├─ Salt rounds: 10                                    │
│  └─ Time: ~100ms per hash (safe from brute force)     │
│                                                         │
│  HTTPS/TLS:                                             │
│  ├─ Support: Configurable certificates                │
│  ├─ Production: TLS_CERT + TLS_KEY env vars           │
│  └─ Headers: HSTS enforces HTTPS                       │
│                                                         │
│  Security Headers:                                      │
│  ├─ HSTS: max-age=31536000 (1 year)                   │
│  ├─ CSP: default-src 'self'                           │
│  ├─ X-Frame-Options: DENY                             │
│  └─ X-XSS-Protection: 1; mode=block                   │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Layer 3: Input Validation & XSS Prevention
```
┌─────────────────────────────────────────────────────────┐
│           Injection Prevention Framework                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  Step 1: Input Validation (Whitelist)                  │
│  ├─ Email: RFC-compliant format                        │
│  ├─ Password: 8+ chars + upper/lower/number/special   │
│  ├─ Name: Letters/spaces/apostrophes/hyphens only      │
│  ├─ ObjectId: 24-char hex format                       │
│  ├─ Coordinates: Valid lat/lon ranges                  │
│  └─ Dates: ISO 8601 format                            │
│                                                         │
│  Step 2: Input Sanitization                           │
│  ├─ removeXSS(): Strips <>"'`/javascript:/event       │
│  ├─ escapeHtml(): Entity encoding (&lt; &gt;)         │
│  └─ Recursive: Clean all string fields                │
│                                                         │
│  Step 3: NoSQL Injection Prevention                    │
│  ├─ Mongoose parameterized queries ✅                 │
│  ├─ No string concatenation                           │
│  └─ ObjectId validation before DB query               │
│                                                         │
│  Step 4: Output Encoding                              │
│  ├─ Response fields HTML-encoded                       │
│  ├─ Prevents stored XSS                               │
│  └─ Safe for JSON responses                           │
│                                                         │
│  Result: 30+ protected endpoints                       │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Layer 4: Data Integrity & File Security
```
┌─────────────────────────────────────────────────────────┐
│      Software & Data Integrity Framework                │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  File Upload Security:                                  │
│  ├─ Size Validation: 10 MB max                         │
│  ├─ MIME Type: Whitelist-based                         │
│  ├─ Extension: .jpg/.png/.pdf/.csv/.json only         │
│  ├─ Sanitization: Remove special chars, limit length   │
│  ├─ Path Traversal: Prevention & validation            │
│  ├─ Hashing: SHA-256 for integrity                     │
│  └─ Storage: Secure upload directory                   │
│                                                         │
│  Data Integrity:                                        │
│  ├─ Checksum: SHA-256 hash calculation                │
│  ├─ Signature: HMAC-SHA256 verification                │
│  ├─ Metadata: Track version + timestamp                │
│  ├─ Tampering: Detect unauthorized changes             │
│  └─ Audit Trail: Previous hash stored                  │
│                                                         │
│  Database Safety:                                       │
│  ├─ Transactions: Atomic multi-operation support       │
│  ├─ Rollback: Automatic on error                       │
│  ├─ Consistency: All-or-nothing semantics              │
│  └─ Monitoring: Log all integrity events               │
│                                                         │
│  Dependency Management:                                 │
│  ├─ Lock File: package-lock.json required              │
│  ├─ Script Audit: Check for malicious npm hooks        │
│  ├─ Quality Check: Deprecated packages flagged         │
│  ├─ Dangerous Code: eval/Function scanning             │
│  └─ Version Ranges: Overly permissive flagged          │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

---

## 📊 Data Flow: User Registration

```
                    POST /api/signup
                    {email, password, fullname}
                            │
                            ▼
                ┌─────────────────────────┐
                │  Validation Schemas     │
                │  ✅ Email format OK?    │
                │  ✅ Password strong?    │
                │  ✅ Name valid?         │
                └────────┬────────────────┘
                         │
         ┌───────────────┴───────────────┐
         │ FAIL                          │ PASS
         ▼                               ▼
    400 Error                    ┌──────────────────┐
    Invalid Input                │ Sanitization     │
                                │ removeXSS()      │
                                │ toLowerCase()    │
                                └────────┬─────────┘
                                         │
                                         ▼
                            ┌────────────────────────┐
                            │ Check Existing User    │
                            │ User.findOne({email})  │
                            └────────┬───────────────┘
                                     │
                    ┌────────────────┴────────────────┐
                    │ EXISTS                          │ NEW USER
                    ▼                                 ▼
                409 Conflict                  ┌──────────────────┐
                Email taken                   │ Hash Password    │
                                             │ bcrypt(10 rounds)│
                                             └────────┬─────────┘
                                                      │
                                                      ▼
                                         ┌────────────────────────┐
                                         │ Create Database Record │
                                         │ - email                │
                                         │ - hashedPassword       │
                                         │ - fullname (sanitized) │
                                         │ - role: 'researcher'   │
                                         │ - timestamps           │
                                         └────────┬───────────────┘
                                                  │
                                                  ▼
                                         ┌────────────────────────┐
                                         │ Generate JWT Token     │
                                         │ jwt.sign({id, role})   │
                                         └────────┬───────────────┘
                                                  │
                                                  ▼
                                         ┌────────────────────────┐
                                         │ Encode Response        │
                                         │ escapeHtml()           │
                                         └────────┬───────────────┘
                                                  │
                                                  ▼
                                         ┌────────────────────────┐
                                         │ Log Success Event      │
                                         │ ✅ User registered     │
                                         │ [email], [IP]          │
                                         └────────┬───────────────┘
                                                  │
                                                  ▼
                                         201 Created
                                         {token, user}
```

---

## 🔍 Data Flow: File Upload

```
                    POST /api/upload
                    File: image.jpg
                            │
                            ▼
            ┌────────────────────────────┐
            │  File Upload Validation    │
            │  Middleware                │
            └────────┬────────────────────┘
                     │
         ┌───────────┼───────────┬──────────────┐
         │           │           │              │
         ▼           ▼           ▼              ▼
    Size Check  Type Check  Ext Check   MIME Check
    < 10 MB?    is jpg?     .jpg OK?    image/jpeg?
         │           │           │              │
         └───────────┼───────────┴──────────────┘
                     │
                     ▼ (All pass)
            ┌────────────────────────────┐
            │  Filename Sanitization     │
            │  input: "../../etc/passwd" │
            │  output: "etc_passwd_...jpg│
            └────────┬────────────────────┘
                     │
                     ▼
            ┌────────────────────────────┐
            │  Save to Disk              │
            │  /backend/uploads/...jpg   │
            └────────┬────────────────────┘
                     │
                     ▼
            ┌────────────────────────────┐
            │  Calculate SHA-256 Hash    │
            │  a3b0c42989... (64 chars) │
            └────────┬────────────────────┘
                     │
                     ▼
            ┌────────────────────────────┐
            │  Store File Metadata       │
            │ - filename                 │
            │ - hash                     │
            │ - size                     │
            │ - mimetype                 │
            └────────┬────────────────────┘
                     │
                     ▼
            ┌────────────────────────────┐
            │  Log Upload Success        │
            │  ✅ File saved (16 KB)     │
            └────────┬────────────────────┘
                     │
                     ▼
            200 OK
            {filename, hash, size}
```

---

## 📈 Deployment Architecture

```
┌────────────────────────────────────────────────────────┐
│                    Production Server                   │
│                                                        │
│  ┌──────────────────────────────────────────────────┐ │
│  │  Environment Variables (from .env or system)     │ │
│  │  - NODE_ENV=production                           │ │
│  │  - JWT_SECRET=xxxxx...                          │ │
│  │  - MONGO_URI=xxxxx...                           │ │
│  │  - TLS_CERT=/path/to/cert                       │ │
│  │  - TLS_KEY=/path/to/key                         │ │
│  └──────────────────────────────────────────────────┘ │
│                         │                              │
│                         ▼                              │
│  ┌──────────────────────────────────────────────────┐ │
│  │  Express Application (server.js)                │ │
│  │                                                 │ │
│  │  ┌────────────────────────────────────────────┐ │ │
│  │  │  Security Middleware Stack                │ │ │
│  │  │  - CORS                                   │ │ │
│  │  │  - Security Headers                       │ │ │
│  │  │  - Request Logging                        │ │ │
│  │  │  - Integrity Helpers                      │ │ │
│  │  │  - Input Validation                       │ │ │
│  │  │  - Authentication                         │ │ │
│  │  │  - Authorization                          │ │ │
│  │  └────────────────────────────────────────────┘ │ │
│  │                                                 │ │
│  │  ┌────────────────────────────────────────────┐ │ │
│  │  │  API Routes                               │ │ │
│  │  │  - /api/auth/* (auth operations)          │ │ │
│  │  │  - /api/markers/* (marker CRUD)           │ │ │
│  │  │  - /api/events/* (event CRUD)             │ │ │
│  │  │  - /api/admin/* (admin operations)        │ │ │
│  │  └────────────────────────────────────────────┘ │ │
│  │                                                 │ │
│  │  ┌────────────────────────────────────────────┐ │ │
│  │  │  Logging System (Winston)                 │ │ │
│  │  │  - combined.log (all events)              │ │ │
│  │  │  - error.log (errors only)                │ │ │
│  │  │  - Rotation: Daily                        │ │ │
│  │  │  - Retention: 14 days                     │ │ │
│  │  └────────────────────────────────────────────┘ │ │
│  └──────────────────────────────────────────────────┘ │
│                         │                              │
│            ┌────────────┴────────────┐                 │
│            │                         │                 │
│            ▼                         ▼                 │
│  ┌──────────────────────┐  ┌──────────────────────┐  │
│  │  MongoDB Atlas       │  │  HTTPS/TLS           │  │
│  │  (Encrypted)         │  │  (Certificates)      │  │
│  │  - Users             │  │  - Port 443          │  │
│  │  - Markers           │  │  - HSTS enabled      │  │
│  │  - Events            │  │  - CSP enabled       │  │
│  │  - Backups           │  │  - Security headers  │  │
│  └──────────────────────┘  └──────────────────────┘  │
│                                                        │
└────────────────────────────────────────────────────────┘
```

---

## 🎯 Security Decision Matrix

| Scenario | Decision | Implementation |
|----------|----------|-----------------|
| User attempts weak password | ❌ REJECT | Validation schema requires complexity |
| Attacker uploads .exe file | ❌ REJECT | MIME type + extension whitelist |
| XSS payload in name field | ✅ SANITIZE | removeXSS() + escapeHtml() |
| SQL injection in query | ✅ PREVENT | Mongoose parameterized queries |
| Unauthorized file access | ❌ DENY | Path traversal prevention |
| Data tampering detected | 🚨 ALERT | Logging + integrity metadata |
| Dangerous npm script | ❌ BLOCK | Dependency audit on startup |
| Missing secret in env | ❌ EXIT | Required env var validation |
| Invalid JWT token | ❌ DENY | Token verification middleware |
| Sensitive data in logs | ✅ REDACT | scrub() function on fields |

---

**Architecture Version:** 1.0  
**Date:** November 20, 2025  
**Status:** Production Ready
